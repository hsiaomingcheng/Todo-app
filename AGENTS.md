# AGENTS.md

This file provides guidance to Codex (Codex.ai/code) when working with code in this repository.

**All project guidance lives in [`CLAUDE.md`](CLAUDE.md) — read that file instead.** It's kept as the single source of truth so instructions don't drift out of sync between AI tools; this file exists only so Codex (which looks for `AGENTS.md` by convention) knows where to find them.
